# Packages really does get messed up sometimes, so saving a command here in case we need to reinstall everything.
# Will publish conda environment file after deciding on main dependencies
conda install -c conda-forge -c anaconda tesseract pytesseract pillow requests pdf2image poppler
